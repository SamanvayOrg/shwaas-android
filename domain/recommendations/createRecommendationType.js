export default (
  shortMessageKey,
  startingMessageKey,
  shortMessageTextAndOuterBoxColor,
  nextSteps,
) => ({
  shortMessageKey,
  startingMessageKey,
  shortMessageTextAndOuterBoxColor,
  nextSteps,
});
